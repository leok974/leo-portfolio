# minimal deps for backend API + unit tests (no heavy ML)
fastapi>=0.117
uvicorn>=0.37
httpx>=0.28
pydantic>=2.11
pytest>=8.4
pytest-asyncio>=1.2
requests>=2.32
python-dotenv>=1.1
jinja2>=3.1
# add lightweight deps actually imported in tests; avoid torch/transformers/faiss/sentence-transformers
