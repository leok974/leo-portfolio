// Custom runtime-injected globals (augment if build tool defines more)
// Declared as ambient so both TypeScript and ESLint (via parser) recognize them.
declare const indexHTML: string;
declare const dirname: string;
