# DEPRECATED: Use shared tunnel 08d5feee-f504-47a2-a1f2-b86564900991 instead
# This config references an old tunnel and should not be used
# See docker-compose.cloudflared.yml for correct configuration

tunnel: DEPRECATED_DO_NOT_USE
credentials-file: /etc/cloudflared/token

ingress:
  - hostname: assistant.ledger-mind.org
    service: http://nginx:80
  - service: http_status:404
