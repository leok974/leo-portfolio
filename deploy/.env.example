# Example environment overrides for full stack compose
# Copy to .env (same directory) and adjust as needed.

EDGE_PORT=8080
FRONTEND_IMAGE=leo-portfolio/frontend:dev
EDGE_IMAGE=leo-portfolio/edge:dev

# Optional: clean source URLs in assistant replies
# RAG_REPO_WEB=https://github.com/leok974/leo-portfolio
# RAG_REPO_REF=main

# Local Playwright testing defaults
# BASE_URL=http://127.0.0.1:8080  # nginx/prod preview
# # If using Vite dev server locally instead of edge:
# # BASE_URL=http://127.0.0.1:5173
