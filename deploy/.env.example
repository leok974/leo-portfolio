# Example environment overrides for full stack compose
# Copy to .env (same directory) and adjust as needed.

EDGE_PORT=8080
FRONTEND_IMAGE=leo-portfolio/frontend:dev
EDGE_IMAGE=leo-portfolio/edge:dev
