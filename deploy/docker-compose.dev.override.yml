# Dev override: mount local dist build and relaxed nginx config
version: "3.9"
services:
  nginx:
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./deploy/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
