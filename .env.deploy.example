# Deployment environment template
# Copy to .env (not committed) and adjust.

# Domain / public hostname
DOMAIN=assistant.ledger-mind.org

# Cloudflare Tunnel (token + UUID mode)
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_WITH_CONNECTOR_TOKEN
CLOUDFLARE_TUNNEL_UUID=db56892d-4879-4263-99bf-202d46b6aff9

# Models
PRIMARY_MODEL=gpt-oss:20b
OPENAI_MODEL=gpt-oss:20b
OPENAI_BASE_URL=http://ollama:11434/v1

# Fallback provider
FALLBACK_BASE_URL=https://api.openai.com/v1
FALLBACK_MODEL=gpt-4o-mini
# Provide via Docker secret in prod; env only for dev
# OPENAI_API_KEY=sk-...

# CORS / Origins (backend augments with DOMAIN variants automatically)
ALLOWED_ORIGINS=https://leok974.github.io,http://localhost:8080,https://assistant.ledger-mind.org

# RAG settings
RAG_DB=./data/rag.sqlite
RAG_REPOS=leok974/ledger-mind,leok974/leo-portfolio
EMBED_MODEL_QUERY=openai/text-embedding-3-large

# Optional startup tuning
# MODEL_WAIT_MAX_SECONDS=180
# DISABLE_PRIMARY=1
