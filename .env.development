# Development Environment Variables
# Copy to .env.local and customize

# Feature Flags
VITE_FEATURE_AGENT_UPLOADS=1

# Backend will use these:
# FEATURE_UPLOADS=1          # Enable uploads (auto-enabled in dev with ALLOW_TOOLS=1)
# MAX_IMAGE_MB=30            # Max image size in MB
# MAX_VIDEO_MB=200           # Max video size in MB
# ALLOW_TOOLS=1              # Dev mode (enables uploads + admin features)
