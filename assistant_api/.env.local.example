# Local DEV ONLY – do not commit real keys.
# Copy to .env.local and fill values for quick local runs.

# RAG
RAG_DB=./data/rag.sqlite
RAG_REPOS=leoklemet/ledger-mind,leoklemet/leo-portfolio

# Use OpenAI for query-time embeddings (Hybrid)
EMBED_MODEL_QUERY=openai/text-embedding-3-large
OPENAI_API_KEY=sk-...             # dev only; in prod use Docker secrets

# LLM primary (Ollama via OpenAI-compatible API)
OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_MODEL=qwen2.5:7b-instruct-q4_K_M
OPENAI_API_KEY_OLLAMA=ollama

# LLM fallback (OpenAI)
FALLBACK_BASE_URL=https://api.openai.com/v1
FALLBACK_MODEL=gpt-4o-mini
FALLBACK_API_KEY=sk-...           # dev only; in prod use Docker secrets

# CORS
ALLOWED_ORIGINS=http://localhost:4173,http://localhost:8000
